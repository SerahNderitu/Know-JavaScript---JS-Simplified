// find index of m
"document".indexOf("m") // 4

// a word with the same characters, indexOf returns the first letter
"process".indexOf("s") // 5

// returns -1 for the character that doesn't exist
"code".indexOf("m") // -1
