// arrow function example used to create little functions

(x,y) => x + y;

// arrow functions assigned to a variable

const add = (x,y) => (x + y);

//  before arrow funtion, the above expression would be like

function add(x,y) {
  return x + y;
}
